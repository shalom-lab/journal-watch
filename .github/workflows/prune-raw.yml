# Delete raw batches older than RAW_RETENTION_MONTHS
name: prune:raw

on:
  workflow_dispatch:
    inputs:
      skip_commit:
        description: "Skip committing data/ back to the repo"
        type: boolean
        default: false
  workflow_call:
    inputs:
      skip_commit:
        description: "Skip committing data/ back to the repo"
        type: boolean
        required: false
        default: false

permissions:
  contents: write

# Do NOT set concurrency here: when called via workflow_call from pipeline,
# github.workflow resolves to the caller name and shared groups deadlock.

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      RAW_RETENTION_MONTHS: ${{ vars.RAW_RETENTION_MONTHS || '3' }}
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-pnpm
      - run: pnpm prune:raw
      - name: Commit data
        if: ${{ inputs.skip_commit != true }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --staged --quiet; then
            echo "No data changes"
          else
            git commit -m "chore(data): prune:raw $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi
